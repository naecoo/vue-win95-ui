<script setup>
import { isZh } from "../i18n";
import { ref } from "vue";
import { W95Window, W95Dialog, W95Button, W95StatusBarField, W95Accordion, W95AccordionItem } from "vue-win95";

const open = ref(false);
const acc = ref("a");
function noop() {}
</script>

<div v-if="isZh">

# Window / Dialog / Accordion

窗口是 Win95 的灵魂：标题栏渐变、控制按钮、状态栏。Dialog 带焦点陷阱与 Esc 关闭。

</div>
<div v-else>

# Window / Dialog / Accordion

Windows capture the Win95 soul: titlebar gradient, control glyphs, status bar. Dialogs trap focus and close on Esc.

</div>

<div class="demo">
  <div class="demo-label">WINDOW + STATUS BAR</div>
  <W95Window title="Task Manager" :width="400" show-minimize show-maximize @close="noop">
    <p style="font-size:14px;margin:0 0 10px">
      {{ isZh ? "进程运行正常。" : "Processes are running smoothly." }}
    </p>
    <W95Button @click="open = true">{{ isZh ? "打开对话框" : "Open Dialog" }}</W95Button>
    <template #status>
      <W95StatusBarField>Press F1 for help</W95StatusBarField>
      <W95StatusBarField>CPU: 14%</W95StatusBarField>
    </template>
  </W95Window>
</div>

<div class="demo">
  <div class="demo-label">DIALOG</div>
  <W95Dialog v-model="open" title="Confirm" :width="520" @confirm="open = false">
    <p style="font-size:14px;margin:0">
      {{ isZh ? "确定要继续吗？" : "Are you sure you want to continue?" }}
    </p>
  </W95Dialog>
  <W95Accordion v-model="acc" style="margin-top:8px">
    <W95AccordionItem value="a" :title="isZh ? '常规' : 'General'">
      {{ isZh ? "系统设置。" : "System settings." }}
    </W95AccordionItem>
    <W95AccordionItem value="b" :title="isZh ? '外观' : 'Appearance'">
      {{ isZh ? "主题选项。" : "Theme options." }}
    </W95AccordionItem>
  </W95Accordion>
</div>

<div v-if="isZh">

## 无障碍

| 能力 | 实现 |
|------|------|
| 焦点陷阱 | `useFocusTrap` |
| 模态 | `aria-modal="true"` |
| 关闭 | Esc / OK / Cancel / 标题栏 Close |
| Accordion | `aria-expanded` + region |

API 速查见 **All Components**。

</div>
<div v-else>

## Accessibility

| Capability | Implementation |
|------|------|
| Focus trap | `useFocusTrap` |
| Modal | `aria-modal="true"` |
| Dismiss | Esc / OK / Cancel / titlebar Close |
| Accordion | `aria-expanded` + region |

See **All Components** for API details.

</div>

<style>
.demo { background:#008080;padding:14px;margin:8px 0; }
.demo-label { color:#fff;font-weight:bold;margin-bottom:8px;font-size:12px;font-family:"Pixelated MS Sans Serif",sans-serif; }
</style>
