<script setup>
import { W95Window, W95Button } from "vue-win95";
import { isZh } from "../i18n";
</script>

<div v-if="isZh">

# 安装

## 环境要求

- Node.js 18 及以上
- Vue 3.4 及以上（对等依赖）

</div>
<div v-else>

# Install

## Requirements

- Node.js 18+
- Vue 3.4+ (peer dependency)

</div>

```bash
pnpm add vue-win95-ui
```

<div v-if="isZh">

## 全局注册

```ts
import { createApp } from "vue";
import Win95 from "vue-win95";
import "vue-win95/styles.css";

createApp(App).use(Win95).mount("#app");
```

## 按需引入

```vue
<script setup lang="ts">
import { W95Button, W95Window } from "vue-win95";
import "vue-win95/styles.css";
</script>
```

样式文件每个应用只需引入一次。

## 自动导入

```bash
pnpm add -D unplugin-vue-components
```

```ts
import Components from "unplugin-vue-components/vite";
import { VueWin95Resolver } from "@win95/vue-resolver";

export default {
  plugins: [Components({ resolvers: [VueWin95Resolver()] })],
};
```

## Tailwind 预设

```ts
import preset from "@win95/tailwind-preset";
export default { presets: [preset], content: [/* … */] };
```

## 安装自检

</div>
<div v-else>

## Global install

```ts
import { createApp } from "vue";
import Win95 from "vue-win95";
import "vue-win95/styles.css";

createApp(App).use(Win95).mount("#app");
```

## On-demand import

```vue
<script setup lang="ts">
import { W95Button, W95Window } from "vue-win95";
import "vue-win95/styles.css";
</script>
```

Import the stylesheet once per application.

## Auto-import

```bash
pnpm add -D unplugin-vue-components
```

```ts
import Components from "unplugin-vue-components/vite";
import { VueWin95Resolver } from "@win95/vue-resolver";

export default {
  plugins: [Components({ resolvers: [VueWin95Resolver()] })],
};
```

## Tailwind preset

```ts
import preset from "@win95/tailwind-preset";
export default { presets: [preset], content: [/* … */] };
```

## Smoke test

</div>

<div class="demo">
  <div class="demo-label">SMOKE TEST</div>
  <W95Window title="Install Check" :width="300">
    <p v-if="isZh" style="font-size:13px;margin:0 0 8px">若能看到窗口与按钮，说明安装成功。</p>
    <p v-else style="font-size:13px;margin:0 0 8px">If you see this window and button, install works.</p>
    <W95Button default>OK</W95Button>
  </W95Window>
</div>

<style>
.demo { background:#008080;padding:14px;margin:8px 0; }
.demo-label { color:#fff;font-weight:bold;margin-bottom:8px;font-size:12px;font-family:"Pixelated MS Sans Serif",sans-serif; }
</style>
