<script setup>
import { W95Window, W95Button, W95Avatar } from "vue-win95";
import { isZh } from "../i18n";
</script>

<div v-if="isZh">

# vue-win95

**Windows 95 风格** 的 Vue 3 组件库。开箱即用的复古桌面 UI，同时保证现代无障碍实践。

</div>
<div v-else>

# vue-win95

A **Windows 95 style** Vue 3 component library. Retro desktop UI out of the box, with modern accessibility.

</div>

<div class="demo">
  <div class="demo-label">LIVE PREVIEW</div>
  <W95Window title="vue-win95" :width="360">
    <div style="display:flex;gap:10px;align-items:center;margin-bottom:10px">
      <W95Avatar initials="W9" size="lg" />
      <div>
        <div style="font-weight:bold;font-size:14px">Hello, Windows 95!</div>
        <div style="opacity:0.85">Vue 3 · TypeScript · Tailwind</div>
      </div>
    </div>
    <W95Button default>OK</W95Button>
  </W95Window>
</div>

<div v-if="isZh">

## 组件库内容

约 **45 个** 组件 / API，覆盖桌面应用常见界面：

| 分类 | 组件 |
|------|------|
| 基础 | Button, Input, Checkbox, Radio, Select, Slider, Switch, Avatar, Icon, Link, Skeleton |
| 容器 | Window, Dialog, GroupBox, Accordion, StatusBar, Divider, Popover, ResizablePanel |
| 导航 | Tabs, Menu, Toolbar, Tree, Breadcrumb, Pagination, Splitter, ContextMenu |
| 浮层 | Tooltip, Toast, Dropdown, ComboBox, Notification |
| 数据 | Table, SpinButton, DatePicker, ProgressBar, Upload |
| API | MessageBox (`W95Alert`/`W95Confirm`), `notify()`, composables |

## 技术栈

- **Vue 3** + Composition API + `<script setup>`
- **TypeScript** 严格类型
- **Tailwind CSS**（shadcn 模式）+ `cn()`
- **主题**：CSS 变量 `--w95-*`
- **无障碍**：W3C APG
- **视觉**：双层 inset 边框、像素字体、标题栏渐变

## 快捷入口

- 安装 → **Install**
- 全部组件与 API → **All Components**
- 更新日志与赞助 → **Changelog & Sponsor**

</div>
<div v-else>

## What's inside

~**45** components/APIs for desktop-style UIs:

| Group | Components |
|------|------------|
| Basics | Button, Input, Checkbox, Radio, Select, Slider, Switch, Avatar, Icon, Link, Skeleton |
| Containers | Window, Dialog, GroupBox, Accordion, StatusBar, Divider, Popover, ResizablePanel |
| Navigation | Tabs, Menu, Toolbar, Tree, Breadcrumb, Pagination, Splitter, ContextMenu |
| Overlays | Tooltip, Toast, Dropdown, ComboBox, Notification |
| Data | Table, SpinButton, DatePicker, ProgressBar, Upload |
| APIs | MessageBox (`W95Alert`/`W95Confirm`), `notify()`, composables |

## Stack

- **Vue 3** + Composition API + `<script setup>`
- **TypeScript** strict typings
- **Tailwind CSS** (shadcn pattern) + `cn()`
- **Theme**: CSS variables `--w95-*`
- **A11y**: W3C APG patterns
- **Look**: dual inset borders, pixel font, titlebar gradient

## Shortcuts

- Install → **Install**
- All components & API → **All Components**
- Changelog & sponsors → **Changelog & Sponsor**

</div>

<style>
.demo { background:#008080;padding:14px;margin:8px 0; }
.demo-label { color:#fff;font-weight:bold;margin-bottom:8px;font-size:12px;font-family:"Pixelated MS Sans Serif",sans-serif; }
</style>
