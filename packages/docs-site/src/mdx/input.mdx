<script setup>
import { isZh } from "../i18n";
import {
  W95Button,
  W95Input,
  W95Checkbox,
  W95Radio,
  W95RadioGroup,
  W95Select,
  W95Slider,
  W95SpinButton,
  W95ComboBox,
  W95FieldRow,
  W95GroupBox,
  W95Divider,
  W95ProgressBar,
} from "vue-win95";
import { ref } from "vue";

const name = ref("World");
const rating = ref("3");
const volume = ref(5);
const copies = ref(2);
const driver = ref(null);
const progress = ref(40);

const ratings = [
  { value: "5", label: "5 - Incredible!" },
  { value: "4", label: "4 - Great!" },
  { value: "3", label: "3 - Pretty good" },
];
const drivers = [
  { value: 1, label: "MySQL ODBC 3.51 Driver" },
  { value: 2, label: "SQL Server" },
];
</script>

<div v-if="isZh">

# Form 表单控件

Input / Select / Slider / SpinButton / ComboBox / FieldRow / GroupBox / ProgressBar。全部基于语义 HTML + ARIA。

</div>
<div v-else>

# Form controls

Input / Select / Slider / SpinButton / ComboBox / FieldRow / GroupBox / ProgressBar. Semantic HTML + ARIA.

</div>

<div class="demo">
  <div class="demo-label">LIVE FORM</div>
  <div style="display:flex;flex-direction:column;gap:8px">
    <W95Input v-model="name" :label="isZh ? '姓名' : 'Your name'" />
    <W95Select v-model="rating" :options="ratings" :label="isZh ? '评分' : 'Rating'" />
    <W95Slider v-model="volume" :min="1" :max="11" :label="isZh ? '音量：' : 'Volume:'" />
    <div style="display:flex;gap:16px;flex-wrap:wrap">
      <W95SpinButton v-model="copies" :min="0" :max="10" :label="isZh ? '份数' : 'Copies'" />
    </div>
    <W95ComboBox v-model="driver" :options="drivers" :label="isZh ? '驱动' : 'Driver'" />
  </div>
</div>

<div v-if="isZh">

**状态**：name=`{{ name }}` · rating=`{{ rating }}` · volume=`{{ volume }}` · copies=`{{ copies }}`

## API 摘要

| 组件 | 关键 Props |
|------|-----------|
| `W95Input` | `modelValue` `type` `label` `disabled` `readonly` |
| `W95Select` | `modelValue` `options` `label` |
| `W95Slider` | `modelValue` `min` `max` `step` |
| `W95SpinButton` | `modelValue` `min` `max` `step` |
| `W95ComboBox` | `modelValue` `options` `loading` `@search` |
| `W95ProgressBar` | `value` `max` `segmented` `label` |

完整列表见 **All Components**。

</div>
<div v-else>

**State**：name=`{{ name }}` · rating=`{{ rating }}` · volume=`{{ volume }}` · copies=`{{ copies }}`

## API summary

| Component | Key props |
|------|-----------|
| `W95Input` | `modelValue` `type` `label` `disabled` `readonly` |
| `W95Select` | `modelValue` `options` `label` |
| `W95Slider` | `modelValue` `min` `max` `step` |
| `W95SpinButton` | `modelValue` `min` `max` `step` |
| `W95ComboBox` | `modelValue` `options` `loading` `@search` |
| `W95ProgressBar` | `value` `max` `segmented` `label` |

See **All Components** for the full catalog.

</div>

<style>
.demo { background:#008080;padding:14px;margin:8px 0; }
.demo-label { color:#fff;font-weight:bold;margin-bottom:8px;font-size:12px;font-family:"Pixelated MS Sans Serif",sans-serif; }
</style>
