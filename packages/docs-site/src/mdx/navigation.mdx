<script setup>
import { isZh } from "../i18n";
import { ref } from "vue";
import {
  W95Tabs, W95TabList, W95Tab, W95TabPanel,
  W95MenuBar, W95MenuItem, W95MenuSubmenu,
  W95Toolbar, W95Button,
  W95Tree, W95TreeItem,
  W95Breadcrumb, W95Pagination,
} from "vue-win95";

const tab = ref("desktop");
const tree = ref("css");
const page = ref(2);
function noop() {}
const crumbs = [{ label: "Desktop", click: noop }, { label: "My Computer", click: noop }, { label: "C:" }];
</script>

<div v-if="isZh">

# Navigation 导航

Tabs · Menu · Toolbar · Tree · Breadcrumb · Pagination。键盘与 ARIA 对齐 W3C APG。

</div>
<div v-else>

# Navigation

Tabs · Menu · Toolbar · Tree · Breadcrumb · Pagination. Keyboard & ARIA follow W3C APG.

</div>

<div class="demo">
  <div class="demo-label">MENU + TOOLBAR + TABS</div>
  <div style="background:#c0c0c0;padding:4px">
    <W95MenuBar>
      <W95MenuItem>{{ isZh ? "文件" : "File" }}</W95MenuItem>
      <W95MenuSubmenu :label="isZh ? '导出' : 'Export'">
        <W95MenuItem>CSV</W95MenuItem>
        <W95MenuItem>PDF</W95MenuItem>
      </W95MenuSubmenu>
      <W95MenuItem disabled>{{ isZh ? "帮助" : "Help" }}</W95MenuItem>
    </W95MenuBar>
    <W95Toolbar style="margin-top:4px">
      <W95Button>New</W95Button>
      <W95Button>Open</W95Button>
      <W95Button>Save</W95Button>
    </W95Toolbar>
    <W95Tabs v-model="tab" style="margin-top:6px">
      <W95TabList>
        <W95Tab value="desktop">Desktop</W95Tab>
        <W95Tab value="computer">My Computer</W95Tab>
      </W95TabList>
      <W95TabPanel value="desktop">
        <W95Breadcrumb :items="crumbs" />
        <W95Tree v-model="tree" style="margin-top:8px;max-width:240px">
          <W95TreeItem value="css" label="CSS" has-children>
            <W95TreeItem value="selectors" label="Selectors" />
            <W95TreeItem value="specificity" label="Specificity" />
          </W95TreeItem>
          <W95TreeItem value="js" label="JavaScript" />
        </W95Tree>
      </W95TabPanel>
      <W95TabPanel value="computer">
        <p style="font-size:13px">tree = {{ tree }} · page = {{ page }}</p>
        <W95Pagination v-model="page" :total="40" :page-size="10" />
      </W95TabPanel>
    </W95Tabs>
  </div>
</div>

<div v-if="isZh">

## 键盘

- **Tabs**：← → Home End
- **Menu / Toolbar**：方向键 roving tabindex
- **Tree**：typeahead、← → 展开收起

</div>
<div v-else>

## Keyboard

- **Tabs**: ← → Home End
- **Menu / Toolbar**: arrow keys with roving tabindex
- **Tree**: typeahead, ← → expand/collapse

</div>

<style>
.demo { background:#008080;padding:14px;margin:8px 0; }
.demo-label { color:#fff;font-weight:bold;margin-bottom:8px;font-size:12px;font-family:"Pixelated MS Sans Serif",sans-serif; }
</style>
