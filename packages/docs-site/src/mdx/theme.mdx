<script setup>
import { isZh } from "../i18n";
</script>

<div v-if="isZh">

# Theme Editor

实时修改 `--w95-*` CSS 变量，预览 Win95 皮肤。

</div>
<div v-else>

# Theme Editor

Tweak `--w95-*` CSS variables live.

</div>

<div class="demo">
  <div class="demo-label">COMING IN THEME PAGE</div>
  <p style="font-size:13px;margin:0">
    {{ isZh ? "本页控件会全局修改 CSS 变量。" : "Controls on this page write CSS variables globally." }}
  </p>
</div>

<style>
.demo { background:#008080;padding:14px;margin:8px 0; }
.demo-label { color:#fff;font-weight:bold;margin-bottom:8px;font-size:12px;font-family:"Pixelated MS Sans Serif",sans-serif; }
</style>
