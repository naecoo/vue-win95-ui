<script setup>
import { isZh } from "../i18n";
import { ref } from "vue";
import {
  W95Icon, W95Link, W95Skeleton, W95Upload, W95Popover, W95Notification,
  W95Switch, W95Avatar, W95Button, W95Divider,
  W95Alert, W95Confirm,
} from "vue-win95";

const on = ref(true);
const files = ref([]);
const pop = ref(false);
const notif = ref([
  { id: 1, title: "Error", message: "Disk full.", type: "error", duration: 0 },
]);
</script>

<div v-if="isZh">

# Misc 其它

Icon · Link · Skeleton · Upload · Popover · Notification · Switch · Avatar · MessageBox

</div>
<div v-else>

# Misc

Icon · Link · Skeleton · Upload · Popover · Notification · Switch · Avatar · MessageBox

</div>

<div class="demo">
  <div class="demo-label">MIXED</div>
  <div style="display:flex;gap:12px;align-items:center;flex-wrap:wrap;margin-bottom:10px">
    <W95Icon name="folder" :size="20" label="Folder" />
    <W95Avatar initials="W9" />
    <W95Switch v-model="on" :label="isZh ? '开关' : 'Switch'" />
    <W95Link href="#">{{ isZh ? "链接" : "Link" }}</W95Link>
  </div>
  <W95Skeleton :lines="1" />
  <W95Divider style="margin:8px 0" />
  <W95Upload v-model="files" :label="isZh ? '选择文件…' : 'Browse…'" />
  <div style="display:flex;gap:8px;margin-top:10px;flex-wrap:wrap">
    <W95Popover v-model="pop" title="Tip" :width="200">
      <template #trigger>
        <W95Button @click="pop = !pop">Popover</W95Button>
      </template>
      <span style="font-size:13px">{{ isZh ? "气泡内容" : "Popover body" }}</span>
    </W95Popover>
    <W95Button @click="W95Alert(isZh ? '来自 MessageBox' : 'From MessageBox', 'Alert')">Alert</W95Button>
    <W95Button default @click="W95Confirm(isZh ? '确定删除？' : 'Delete file?')">
      {{ isZh ? "确认框" : "Confirm" }}
    </W95Button>
  </div>
  <W95Notification v-model="notif" position="bottom-right" />
</div>

<div v-if="isZh">

命令式通知请用 `useNotifier()` / `notify()`。完整导出列表见 **All Components**。

</div>
<div v-else>

Use `useNotifier()` / `notify()` for imperative toasts. Full exports on **All Components**.

</div>

<style>
.demo { background:#008080;padding:14px;margin:8px 0; }
.demo-label { color:#fff;font-weight:bold;margin-bottom:8px;font-size:12px;font-family:"Pixelated MS Sans Serif",sans-serif; }
</style>
