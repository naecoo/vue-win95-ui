<script setup>
import { isZh } from "../i18n";
import { ref } from "vue";
import { W95Table, W95SpinButton, W95DatePicker, W95ProgressBar, W95Button } from "vue-win95";

const selected = ref(2);
const copies = ref(3);
const date = ref("2024-06-15");
const progress = ref(45);
const columns = [
  { key: "name", label: "Name", width: 140, sortable: true },
  { key: "version", label: "Version", width: 70 },
];
const rows = [
  { id: 1, name: "MySQL ODBC", version: "3.51" },
  { id: 2, name: "SQL Server", version: "3.70" },
];
</script>

<div v-if="isZh">

# Data 数据组件

Table · SpinButton · DatePicker · ProgressBar

</div>
<div v-else>

# Data

Table · SpinButton · DatePicker · ProgressBar

</div>

<div class="demo">
  <div class="demo-label">LIVE DATA</div>
  <W95Table v-model:selected-key="selected" :columns="columns" :rows="rows" :height="90" />
  <div style="display:flex;gap:16px;flex-wrap:wrap;margin-top:10px">
    <W95SpinButton v-model="copies" :min="0" :max="20" :label="isZh ? '份数' : 'Copies'" />
    <W95DatePicker v-model="date" :label="isZh ? '日期' : 'Date'" />
  </div>
  <div style="margin-top:8px">
    <W95ProgressBar :value="progress" :label="isZh ? '进度' : 'Progress'" />
  </div>
  <W95Button style="margin-top:8px" @click="progress = (progress + 15) % 120">+15%</W95Button>
</div>

<div v-if="isZh">

Table 支持排序、多选（Ctrl 点选）、方向键选择。DatePicker 支持 `min`/`max`。

</div>
<div v-else>

Table supports sort, multi-select (Ctrl+click), and arrow-key selection. DatePicker supports `min`/`max`.

</div>

<style>
.demo { background:#008080;padding:14px;margin:8px 0; }
.demo-label { color:#fff;font-weight:bold;margin-bottom:8px;font-size:12px;font-family:"Pixelated MS Sans Serif",sans-serif; }
</style>
