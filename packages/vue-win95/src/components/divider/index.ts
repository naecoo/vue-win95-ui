import { withInstall } from "../../utils/withInstall";
import _Divider from "./Divider.vue";

export const W95Divider = withInstall(_Divider);
export type W95DividerProps = {
  vertical?: boolean;
};
